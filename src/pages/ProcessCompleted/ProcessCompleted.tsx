import { IonContent, IonPage } from '@ionic/react';
import './ProcessCompleted.css';

const ProcessCompleted: React.FC = () => {
    return (
        <IonPage>
            <IonContent>
                <p>Contenido de la página del proceso completado</p>
            </IonContent>
        </IonPage>
    );
};

export default ProcessCompleted;
