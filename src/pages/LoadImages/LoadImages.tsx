import { IonContent, IonPage } from '@ionic/react';
import './LoadImages.css';

const LoadImages: React.FC = () => {
    return (
        <IonPage>
            <IonContent>
                <p>Contenido de la página de carga de imágenes</p>
            </IonContent>
        </IonPage>
    );
};

export default LoadImages;
